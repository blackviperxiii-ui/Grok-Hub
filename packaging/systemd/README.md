# GrokHub agent (systemd user unit)

Optional. Not required for chat.

```bash
./scripts/sync-user-integration.sh --agent --now
```

## Not the same as `~/.grok`

Standalone xAI **`grok` CLI** under `~/.grok/downloads/` is a different product.
GrokHub’s command is always **`grokhub`**.
